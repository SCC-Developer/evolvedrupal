/**
 * @file
 * AL Theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.alTheme = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
